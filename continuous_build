#!/usr/bin/env bash

while true
do
    if [[ "$(git pull)" != "Already"* ]]; then
        notify-send "New commit! Pulling and building"
        vagrant ssh -- "cd teaching; make -j 10 all"
        make deploy
        notify-send "The website has been updated"
    fi
    sleep 300
done
