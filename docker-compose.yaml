version: '3.1'
services:
  make:
    build: .
    user: ${ID}
    volumes:
      - .:/teaching
  manim:
    image: eulertour/manim:latest
    entrypoint:
      - ./manim.py
    user: ${ID}
    volumes:
      - ./videos:/videos
    working_dir: /videos
    network_mode: "none"
